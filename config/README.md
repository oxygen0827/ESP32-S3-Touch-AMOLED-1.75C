# Config

[English](README.md) | [简体中文](README_ZH.md)

This directory is reserved for shared CI or ESP-IDF configuration overlays.

Keep example-specific `sdkconfig.defaults` files with the example that consumes them. Add shared overlays here only when multiple examples use the same settings.
