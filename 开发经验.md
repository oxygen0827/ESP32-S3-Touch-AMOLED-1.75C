# 开发经验

## 2026-08-24：确认 C6 SKU 并建立对应资料目录

- 当前设备 `/dev/cu.usbmodem1101` 被 ROM 识别为 ESP32-C6；使用 S3 仓库会导致目标芯片和板级引脚配置不匹配。
- Waveshare 官方 C6 仓库已拉取到本目录；GitHub 与官网链接的 Gitee 镜像当前内容树一致。
- C6 资料页列出的原理图、尺寸包、C6 Datasheet/TRM 和器件资料已下载，哈希记录见 `resource/资料索引.md`。
- 后续烧录前仍需确认例程对应的分区表、Flash 地址和是否需要擦除旧固件；不要把 S3 固件写入 C6。

## 2026-08-24：官方尺寸包文件名沿用 S3

- **现象**：C6 官方资料页提供的 `ESP32-C6-Touch-AMOLED-2.16-Struct.zip` 内部文件名为 `ESP32-S3-Touch-AMOLED-2.16-Dxf.dxf` 和同名 PDF。
- **处置**：保留官方原始文件名和压缩包内容，不擅自重命名；索引中明确记录该现象。机械尺寸应与 C6 原理图和实物复核，不能仅依据内部文件名判断主控型号。

## 2026-08-24：Arduino 工具链下载超时

- **现象**：`arduino-cli core install esp32:esp32@3.3.3` 在下载官方 `esp32-3.3.3-libs.zip` 时因 GitHub 大文件 HEAD 请求超时退出。
- **处置**：代码和资料下载不受影响；暂不把该网络超时判定为例程编译失败。后续可使用已验证的直链/断点续传方式准备工具链，再执行批量编译。

## 2026-08-24：ESP-IDF 显示例程引脚配置与原理图不一致

- **现象**：C6 ESP-IDF 例程的 9 份 `main/user_config.h` 把 LCD CS 写成 GPIO5、
  触摸 INT 写成 GPIO15。`07_Audio_Test`、`08_LVGL_V8_Test` 和 `09_LVGL_V9_Test`
  的 `DisplayPort` 默认值则是 `cs = 15`、`tp_int = 5`，因此两套配置互相矛盾；
  编译不会报告该板级错误。
- **核对**：从 C6 原理图 PDF 的网络表（J4/U2）确认 `NLGPIO15 -> NLLCD0CS`
  （LCD CS），`NLGPIO5 -> NLTP0INT`（触摸中断），`GPIO11 -> TP_RESET`。官方
  Arduino 例程也使用 CS15/INT5，与原理图和 IDF `DisplayPort` 默认值一致。
- **修复**：将 9 份 ESP-IDF `user_config.h` 统一改为
  `BSP_LCD_CS = GPIO_NUM_15`、`BSP_LCD_TOUCH_INT = GPIO_NUM_5`；保持三个
  `display_bsp.h` 的默认值不变。修复后需在 C6 实物上验证显示初始化和触摸中断。

## 2026-08-24：C6 Arduino 批量编译工具链与源码兼容性

- Arduino CLI 使用隔离目录 `/tmp/arduino-cli-s3`，核心为 Arduino-ESP32 3.3.11，目标 FQBN 为
  `esp32:esp32:esp32c6`。原隔离环境只有 S3 运行库和 Xtensa 工具链，C6 编译会报缺少
  `esp32c6-libs`、`riscv32-esp-elf-g++`；补齐官方 `esp32c6-libs` 和 `esp-rv32` 后恢复正常。
- Apple ARM 下 Arduino 预处理器未为官方 01–06 示例生成 `Custom_PmicRegisterInit` 原型，C++ 编译报
  “not declared in this scope”。在 01–06 的 `.ino` 文件中加入显式前置声明，避免依赖 ctags 预处理副作用。
- 07 音频例程使用 LVGL 9 API，误选 LVGL 8 会出现 `lv_display_t`、`lv_button_create` 等大量未声明错误；改用
  `01_Arduino_Libraries/lvgl9` 后源码通过。该例程默认分区只有约 1.3 MB，固件约 2.63 MB，需选择
  `FlashSize=16M,PartitionScheme=huge_app`（3 MB APP）才能链接。
- 最终 C6 Arduino 9 个例程均已编译通过：01–06 使用默认分区，08 使用 LVGL 8，09 使用 LVGL 9，07 使用
  LVGL 9 + `huge_app`。编译不等于上板验证，仍需逐个烧录并查看串口及外设初始化日志。

## 2026-08-24：硬件串口恢复与验证边界

- 开发板曾短暂从 macOS 端口列表消失，重新连接后 `/dev/cu.usbmodem1101` 恢复；`esptool` 已确认
  ESP32-C6 QFN40、revision v0.2、16 MB Flash、USB-Serial/JTAG，MAC 为
  `20:6e:f1:ff:fe:16:e0:a4`。
- `esptool` 需要访问 macOS USB 设备权限；沙箱内会误报 `Operation not permitted`，使用已批准的串口访问执行。
- 在完成每个例程烧录后，串口日志需区分“固件启动正常”“外部 SD/音频/Wi-Fi 环境条件不足”和“真正崩溃”；
  未捕获到日志前不能宣称例程已在板上跑通。

## 2026-08-24：C6 ESP-IDF 实机验证结果

- 01 `AXP2101_Test`：PMIC 初始化成功；持续读取充电状态和电池电压，无 panic。
- 02 `I2C_QMI8658`：QMI8658 初始化成功；连续输出稳定加速度/陀螺仪数据，无 panic。
- 03 `I2C_PCF85063`：RTC 初始化成功；时间每秒递增输出，无 panic。
- 04 `SD_Card`：PMIC 正常，但 SD 挂载返回 `ESP_ERR_TIMEOUT (0x107)`；应插入可识别 SD 卡后复测，不能据此判定固件崩溃。
- 05 `WIFI_STA`：Wi-Fi 驱动、PHY 和 STA 模式初始化正常；因环境中没有示例硬编码的 `ESP32_STA` AP 而断开。
- 06 `WIFI_AP`：SoftAP 和 DHCP 正常启动，默认 IP 为 `192.168.4.1`。
- 07 `Audio_Test`：SH8601/CST9217 显示触摸、ES8311/ES7210 codec、I2S TDM、LVGL adapter 和触摸 IRQ 均初始化成功。
- 08 `LVGL_V8_Test`：SH8601 LCD、CST9217 480x480 触摸和 LVGL v8 task 初始化成功。
- 09 `LVGL_V9_Test`：SH8601 LCD、CST9217 480x480 触摸和 LVGL v9 task 初始化成功。
- 原始串口记录保存在仓库外的 `build-c6-idf/hardware-*.log`，烧录产物和构建日志按例程目录保存。

## 2026-08-24：Arduino CDC 串口验证注意事项

- Arduino ESP32-C6 默认 FQBN 选项 `CDCOnBoot=default` 会关闭 USB CDC；固件虽可烧录，但应用 `Serial` 日志不可见。
- 用 `CDCOnBoot=cdc` 重编译后，01 例程可见 `start`、PMIC 和电池状态日志；其余 CDC 版本均已成功编译，待串口权限恢复后逐个烧录验证。
- 07 Arduino 音频例程必须使用 LVGL 9 和 `FlashSize=16M,PartitionScheme=huge_app`；并同时启用 `CDCOnBoot=cdc` 才便于串口判定。
- Arduino 01 的首次默认分区固件可以烧录但只看到 ROM 启动头；改用 `CDCOnBoot=cdc` 后应用日志正常可见并已验证 PMIC。
- 继续烧录 Arduino 02–09 时，macOS USB 设备审批通道返回代理服务错误，导致 `esptool` 暂时无法打开串口；这不是固件编译错误。相关 CDC 固件均已生成，待权限通道恢复后可从 02 继续。

## 2026-08-25：Clara C6 LVGL9 启动页组件

- **现象**：初版启动页的卡片事件直接调用带 `Lvgl_lock` 的页面切换函数；同时 Clara 页面只有一个会改变文案的 Start 按钮，没有独立 Stop 事件，会议停止操作无法与回调接口一一对应。
- **原因**：事件回调本身运行在 LVGL 适配器上下文，页面切换需要区分“已持锁的内部操作”和“外部任务调用”；Start/Stop 状态也不能只靠改标签表达。
- **修复**：在 `03_Clara_C6/main/clara_ui.cpp` 增加锁内 `set_page_locked()`，卡片和 Back/Demo 事件使用该内部函数；Clara 控件增加独立 Stop 按钮并绑定 `stop_meeting`，`clara_ui_set_meeting_active()` 同步按钮禁用状态和文案。初始化阶段统一在 `Lvgl_lock` 保护下创建页面。
- **验证**：使用 ESP-IDF v5.5.3、目标 `esp32c6` 执行 `ninja -C build-clara esp-idf/main/CMakeFiles/__idf_main.dir/clara_ui.cpp.obj`，LVGL9 UI 目标编译通过。该记录只证明 UI 组件编译通过；整机固件仍需等主程序、网络和音频模块完成后再做全量构建及上板验证。

## 2026-08-25：Clara 工程依赖收敛与全量构建边界

- **现象**：全量构建初次访问 `codec_bsp.h` 时找不到 `codec_board.h`；随后 `app_bsp/lvgl_bsp.cpp` 找不到 `esp_timer.h`。这两类错误来自组件公开头文件使用了未导出的依赖，而不是 C6 芯片或 LVGL API 不兼容。
- **处置**：将 `codec_board` 从 `port_bsp` 的私有依赖调整为公开 `REQUIRES`，并为 `app_bsp` 补充 `esp_timer` 依赖；修复后 `main.cpp`、`clara_net.cpp` 和 `clara_ui.cpp` 均可进入编译阶段。
- **当前阻塞**：主程序已把音频逻辑合并到 `main.cpp` 后，`main/CMakeLists.txt` 仍列有 `clara_audio.cpp`；该文件不存在时 CMake 配置阶段报 `Cannot find source file`。应删除该源文件条目或恢复实际实现，再继续全量链接。此次构建未生成可烧录的 Clara 固件，不能据此宣称整机成功。

## 2026-08-25：Clara C6 HTTPS/WSS 网络模块

- **现象**：网络模块初版使用 `snprintf()` 将 Base64 音频从缓冲区原位拼接到 JSON，ESP-IDF 的 `-Werror=restrict` 将源/目的重叠判定为编译错误。
- **原因**：C/C++ 标准库不允许 `snprintf()` 的 `%s` 源字符串与目标缓冲区重叠，即使预留了偏移量也不能依赖该行为。
- **修复**：改为先 Base64 编码，再用 `memmove()` 插入固定 JSON 前缀和后缀；转写通道直接发送 16 kHz/16-bit/mono 原始 binary frame，避免额外 Base64 内存和带宽开销。HTTP/WSS 启用 ESP-IDF CA bundle，事件回调只传递临时数据指针，日志不输出 SSID、密码、主题、会话内容或音频。
- **验证**：`ninja -C 03_Clara_C6/build esp-idf/main/CMakeFiles/__idf_main.dir/clara_net.cpp.obj` 在 ESP-IDF v5.5.3、目标 `esp32c6` 下编译通过。整机链接仍受主程序既有 `codec_board.h` 组件可见性和 UI 其它编译错误影响，网络目标本身无编译错误。

## 2026-08-25：Clara C6 首次整机构建修复

- **现象**：整机 CMake 配置先因 `main/CMakeLists.txt` 引用了不存在的 `clara_audio.cpp` 失败；补齐音频适配器后，`app_bsp/lvgl_bsp.cpp` 又无法找到 `esp_timer.h`，随后 `display_bsp.h` 无法找到 `esp_lcd_touch_cst9217.h`。
- **原因**：组件源文件列表与工作区实际文件不同步；`app_bsp` 没有声明 `esp_timer`；`port_bsp` 的公开头文件包含 CST9217 头文件，却只在 `PRIV_REQUIRES` 声明了该组件，依赖没有传递给使用者。
- **修复**：增加 `main/clara_audio.cpp`，实现 CodecPort/`esp_codec_dev` 的 PCM16 读写、双声道下混、锁保护和可选 MP3 分支；在 `app_bsp` 增加 `esp_timer` 私有依赖；将 `waveshare__esp_lcd_touch_cst9217` 移到 `port_bsp` 的公开 `REQUIRES`；工程 CMake 增加仅在本机存在时加载 `sdkconfig.local` 的逻辑。
- **验证**：ESP-IDF v5.5.3、目标 `esp32c6` 下 `idf.py build` 完整通过，生成 `build/Clara_C6.bin`；当前仅有官方宏初始化和默认配置中 LVGL 旧符号的 warning，没有编译或链接错误。未提供本机 `sdkconfig.local` 时网络配置保持为空，不会把凭据写入源码或日志。

## 2026-08-25：Clara C6 上板 UI 内存池耗尽

- **现象**：固件可以启动并完成 SH8601/CST9217 初始化，但在 `clara_ui.cpp:create_home()` 创建 Clara 卡片头像时，`lv_obj_create()` 返回空指针，随后样式函数触发 Load access fault；网络模块尚未开始连接。
- **原因判断**：当前 `CONFIG_LV_MEM_SIZE_KILOBYTES=64` 的 LVGL 内存池不足以容纳启动页的多卡片控件和字体对象。代码对对象创建结果缺少统一空指针保护，导致分配失败升级为 panic。
- **处置建议**：先将 LVGL 内存池调大（例如 128 KB，确认应用分区和剩余 RAM），并给页面构造器增加关键对象分配检查；重新刷写后再验证 UI 稳定性和网络路径。该条为上板实测问题，不能用“编译通过”替代运行验证。

## 2026-08-25：Clara C6 音频适配器

- **现象**：Clara 网络协议需要 16 kHz、16-bit、单声道 PCM，但 C6 板级 `CodecPort`/`esp_codec_dev` 默认以双声道音频设备打开；原工程的 `clara_audio.cpp` 只有占位实现，无法直接给网络和播放逻辑使用。
- **修复**：新增 `main/clara_audio.h/.cpp`，以 `CodecPort` 提供的输入/输出句柄为边界，统一处理设备打开/关闭、采集双声道下混、单声道到硬件声道展开、16/24/32-bit 样本转换、音量/输入增益和 FreeRTOS 互斥保护；适配器不释放 `CodecPort` 所有的句柄。
- **验证**：使用 ESP-IDF v5.5.3、目标 `esp32c6` 执行 `idf.py build`，Clara_C6 固件完整编译和链接通过，`build/Clara_C6.bin` 已生成。此项验证覆盖编译和链接，尚未替代主线程的最终整机刷写/现场音频测试。
- **MP3 边界**：网络回答若为 MP3，可选接入公开的 `espressif/esp_audio_codec` 组件启用流式解码；未声明该依赖时 MP3 API 明确返回 `ESP_ERR_NOT_SUPPORTED`，PCM 主路径仍可独立构建。接入后应额外验证分片 MP3 输入和实际扬声器播放，不能仅凭编译结果宣称 MP3 已跑通。

## 2026-08-25：Clara C6 首次上板 UI 空指针崩溃

- **现象**：固件烧录成功，PMIC、SH8601、CST9217、LVGL、ES8311/ES7210 均初始化成功；随后设备反复 `Guru Meditation Error: Load access fault`，回溯指向 `clara_ui.cpp:186` 的 `lv_obj_set_local_style_prop(obj=0)`。
- **原因**：首页和 Clara 页面头像对象创建后，样式调用误把对象参数写成整型 `0`；该 C API 能通过编译，但运行时直接解引用空对象。
- **修复**：将两处头像的 `lv_obj_set_style_border_width(0, 0, 0)` 改为传入对应的 `avatar` 对象，并用实机串口回溯确认问题位置。
- **验证**：修复后需重新构建、刷写并观察至少一次完整启动周期；必须确认不再出现 panic，才能继续触摸和会议链路验证。

## 2026-08-25：Clara C6 音频任务栈溢出

- **现象**：修复 UI 空指针后，实机已完成显示、触摸、codec 和 Wi-Fi 驱动初始化，但任务 `clara_audio` 报 `Stack protection fault`，回溯落在 `main.cpp:52` 的音频缓冲区定义。
- **原因**：任务栈配置为 6144 字节，却在栈上分配 `stereo[3200]` 和 `mono[1600]`，仅数组就占用约 9.6 KiB；C6 无 PSRAM，任务启动即越界，即使会议未开始也会崩溃。
- **修复**：将音频块改为 320 帧（16 kHz 下 20 ms），双声道和单声道栈缓冲合计约 1.9 KiB；保持每个块直接发送给转写或 Host WebSocket，降低实时延迟。
- **验证**：将 320 帧版本重新构建并刷写到 `/dev/cu.usbmodem1101` 后，串口完成 PMIC、显示、触摸、codec、Wi-Fi 初始化并打印 `Clara UI ready; touch the Clara tile`，在网络未配置状态下未再因音频任务栈溢出重启。

## 2026-08-25：Clara C6 本地私密配置未覆盖空 sdkconfig

- **现象**：工程目录存在权限为 `600` 的 `sdkconfig.local`，但刷写后的串口仍提示 `Wi-Fi SSID is not configured`。
- **原因**：ESP-IDF 会把已有的 `sdkconfig` 作为当前配置，`SDKCONFIG_DEFAULTS` 只提供默认值，不能覆盖其中显式保存的空字符串；初版 CMake 因此没有把本机 overlay 编译进 `sdkconfig.h`。
- **修复**：`03_Clara_C6/CMakeLists.txt` 在检测到 `sdkconfig.local` 时，将 `build/sdkconfig.private` 设为生成配置，并按 `sdkconfig.defaults`、无凭据的 `sdkconfig`、私密 `sdkconfig.local` 顺序加载；生成完成后用 CMake `file(CHMOD)` 将该文件固定为所有者读写（`600`）。源码中的 `sdkconfig` 保持空值，私密 overlay 不纳入版本控制。
- **验证**：重新 `idf.py reconfigure && idf.py build` 成功；生成配置头中 Wi-Fi SSID、密码、API 地址和 topic 四项均为非空（仅核对长度，未输出值），后续刷写应以串口 `Wi-Fi: connected` 为准继续做真实网络链路验证。

## 2026-08-25：Clara C6 Wi-Fi 启动重复 connect

- **现象**：串口在 STA 启动阶段出现 `wifi:sta is connecting, return error`，随后仍能继续运行。
- **原因**：`clara_net_wifi_start()` 在 `esp_wifi_start()` 返回后立即调用 `esp_wifi_connect()`，而 `WIFI_EVENT_STA_START` 回调也会调用一次；两次调用在同一启动窗口竞争，第二次返回 `ESP_ERR_WIFI_CONN`。
- **修复**：删除 `clara_net_wifi_start()` 中的直接 connect，让 `WIFI_EVENT_STA_START` 成为唯一的首次连接入口；保留重试逻辑在 `WIFI_EVENT_STA_DISCONNECTED`。
- **验证**：代码修改后需重新构建刷写；判断标准是启动日志不再出现该重复连接错误，并在热点可见时收到 `CLARA_NET_EVENT_WIFI_CONNECTED`/`IP_EVENT_STA_GOT_IP`。

## 2026-08-25：Clara C6 触摸回调阻塞 LVGL 任务

- **现象**：Start/Stop/Ask/Refresh 的 LVGL 点击回调直接执行 Wi-Fi 等待、HTTPS 请求和 WSS 握手；网络慢时 UI 任务会长时间无法刷新，虽然递归 LVGL 锁本身不会死锁。
- **原因**：网络 API 是同步接口，事件回调运行在 LVGL adapter 任务；把同步 I/O 放在该上下文会阻塞触摸和渲染。
- **修复**：`main/main.cpp` 新增 `clara_action` FreeRTOS 工作任务和四项动作队列；LVGL 回调只入队，实际会议生命周期、Host 问答和摘要请求在独立任务执行，UI setter 继续通过 `Lvgl_lock` 更新。
- **验证**：需重新 `idf.py build` 并刷写；启动后应同时看到 LVGL 正常运行和音频任务无栈溢出。待热点可用时再通过触摸执行 Start/Stop，确认 UI 在网络等待期间仍可响应。

## 2026-08-25：Clara C6 Wi-Fi 失败诊断保持私密

- **现象**：当前环境没有可确认的热点连接；启动日志只显示 STA 模式开始，无法仅凭“未连上”判断是热点不可见还是认证失败。
- **处置**：在 `main/clara_net.cpp` 的 `WIFI_EVENT_STA_DISCONNECTED` 中增加数值 `reason` 和重试计数日志，不输出 SSID、密码、BSSID 或 IP；这些数值可用于后续现场判断而不泄露私密配置。
- **验证**：重新刷写后实测连续得到 `reason=201`（`WIFI_REASON_NO_AP_FOUND`）并完成 8 次重试；说明当前热点不可见，而不是凭据被打印或固件崩溃。热点可见时应先收到 `CLARA_NET_EVENT_WIFI_CONNECTED`，再继续 HTTPS/WSS 会议链路验证。
- **编译补充**：首次加入诊断字段时，回调第四参数仍是未命名的 `void *`，引用 `event_data` 触发编译错误；已将参数显式命名并重新进入构建验证。
- **外部服务边界**：开发机对配置的健康检查返回 HTTP 200；由于板上实测为 `NO_AP_FOUND`，尚未从 C6 发起 session/转写/Host/understanding 请求，不能把服务端可达性等同于板上全链路成功。

## 2026-08-25：Clara C6 手动重连需重置 Wi-Fi 重试计数

- **现象**：STA 启动时热点不可见会耗尽 8 次自动重试；用户随后打开热点并点 Start，旧计数可能仍为上限，导致只尝试一次就再次报告失败。
- **原因**：`clara_net_wifi_start()` 的“已启动但未连接”分支清除事件位并重新 connect，却没有把 `s_wifi_retries` 归零。
- **修复**：在该分支重新连接前重置 `s_wifi_retries = 0`，允许 UI 触发的新一轮连接完整重试。
- **验证**：重新构建刷写；本次启动仍按 `reason=201` 完成自动重试，后续热点可见时可在不重启设备的情况下再次连接。

## 2026-08-25：非 TTY 环境下的串口监视

- **现象**：在自动化执行通道运行 `idf.py monitor` 时，`idf_monitor` 报 `Monitor requires standard input to be attached to TTY` 并退出；这不是固件或串口权限错误。
- **处置**：使用同一 `/dev/cu.usbmodem1101` 的 pyserial 只读脚本，按 USB-Serial/JTAG 的 DTR/RTS 序列复位后采集启动日志；脚本不发送业务数据，也不打印私密配置。
- **验证**：已连续捕获 ROM、bootloader、PMIC、LCD、触摸、codec、LVGL、Wi-Fi 初始化和 `reason=201` 重试日志；后续需要交互式终端时再使用 `idf.py monitor`。

## 2026-08-25：C6 工作区迁移后的构建缓存路径

- **现象**：将 C6 工程和已编译目录从旧的 S3 工作区迁移到 `ESP32-C6-AMOLED-2.16/` 后，`CMakeCache.txt`、生成的 CMake 文件和构建日志仍记录旧工作区绝对路径，直接在新目录做增量构建可能找不到原工程路径。
- **原因**：ESP-IDF/CMake 会把源目录、构建目录和组件路径写入缓存及生成文件；目录移动不会自动改写这些文本元数据。`build/` 和 `build-clara/` 还被 `.gitignore` 忽略，普通文件扫描容易漏掉它们。
- **修复**：在 C6 工作区内使用不忽略隐藏/构建目录的文本扫描，批量将旧工作区前缀替换为 `/Users/hsh/vibecoding/ESP32-C6-AMOLED-2.16`；未修改 `.bin`、`.elf`、静态库等二进制产物，也未触碰 S3 工作区。
- **验证**：C6 目录内文本和可扫描文件均不再包含旧路径；Clara 的 `build/`、`build-clara/` 的 `CMAKE_HOME_DIRECTORY` 与 `CMAKE_CACHEFILE_DIR` 均指向新目录；已编译例程 64 个 `.bin`、Clara 2 个 `.bin` 保留，`sdkconfig.private` 与 `sdkconfig.local` 权限仍为 `600`。

## 2026-08-26：Clara C6 Wi-Fi 回切 LDKJ 与 TLS 自测故障归因

- **配置结果**：Clara 当前使用之前的 Wi-Fi `LDKJ`（密码仅保存在本机私密配置中）；`03_Clara_C6/sdkconfig.local` 和生成的 `build/sdkconfig.private` 已同步，文件权限保持为 `600`。源码默认配置仍为空，避免把凭据提交到仓库、固件日志或示例文件。
- **配置加载根因**：ESP-IDF 已存在的 `sdkconfig` 会优先保留其中的显式值，单靠 `SDKCONFIG_DEFAULTS` 不能覆盖空的 Wi-Fi 字段；Clara 顶层 CMake 必须在检测到 `sdkconfig.local` 时，将 `sdkconfig.defaults`、无凭据 `sdkconfig`、私密 overlay 按顺序生成 `build/sdkconfig.private`，否则刷写后会继续提示 SSID 未配置。
- **失败现象与根因**：`serial_100s_ldkj_final.log` 已确认设备以 WPA2 成功关联 LDKJ、拿到 DHCP 地址和 DNS；随后 HTTPS 建连期间发生 `bcn_timeout`，Wi-Fi 断开原因为 `200`，紧接着出现 mbedTLS `-0x2700`、HTTP status `0` 和错误 `28674`。抓包和服务端证书核对表明，最核心原因是服务端更新到 2026 年 Let's Encrypt 证书链，而固件内置的旧证书 bundle 无法完成交叉签名链校验；同时初始 mbedTLS 输入缓冲区只有约 2 KiB，小于服务端约 3409 字节的证书消息，且 C6 在校验证书时可用连续内存不足，出现 `PK - Memory allocation failed`。IPv6 在部分 iPhone 热点上还会黑洞，等待约 30 秒后才回退 IPv4。`bcn_timeout`/11n AMPDU 聚合断链是 HTTPS 流量触发时的伴随现象，不是 Wi-Fi 密码错误或首要根因。
- **解决方案**：将服务端实际证书链以 `clara_ca_chain.h` 固定提供给 HTTPS/WSS，避免依赖不完整的系统 bundle；将 mbedTLS 输入/输出缓冲区调到可容纳证书链的大小并关闭保留对端证书，降低校验峰值内存；关闭 IPv6，保持 DHCP DNS 为主解析器、公共 DNS 仅作备用；禁用 Wi-Fi AMPDU、限制 STA 为 11b/g/n 并关闭省电，规避热点兼容性导致的 beacon 丢失；HTTP/WSS 使用较小但足够的应用层缓冲区；启动自测在 Wi-Fi 恢复后重试创建 session，并记录数值化断开原因而不泄露凭据。
- **验证证据**：`serial_100s_final3cert.log` 再次显示连接 LDKJ 后，session 返回 HTTP 200，转写和 Host 两条 WebSocket 均连接成功，启动网络自测完成，说明证书链、缓冲区、IPv4 路径和服务端均已打通。后续若再失败，优先区分 `reason=201`（找不到 AP）、`reason=2`（认证失败）和 `reason=200`（beacon timeout/链路恢复），并结合 TLS 错误和内存日志判断，不能仅凭 HTTP status 0 认定为服务端故障。

## 2026-08-26：Clara 实时转录、结构化摘要和会后问答

- **现象**：Clara 页面把 `/understanding` 返回的完整 JSON 直接放进文本框，屏幕出现 `schemaVersion`、`sessionId`、`revision`、`transcriptCount` 等内部字段；转写事件每次覆盖整段标签，不能形成连续会议记录；停止会议会调用 `/end` 并清空 session ID，导致会后无法继续提问。
- **根因**：UI 没有区分临时转录、最终转录和流式回答 delta；会议录音状态、Host 连接状态、Host 录音状态被一个布尔量混用；摘要渲染也没有按服务器真实 envelope 提取可读字段。服务器实测表明 `/understanding` 的正文位于 `overview.headline`、`understanding.meetingGoal/topics` 和 `decisionState`，而且调用 `/end` 后 Host WebSocket 会拒绝连接，因此“结束录音”和“销毁 session”不能是同一个动作。
- **修复**：UI 维护有上限的最终文本和临时文本缓冲，最终转录追加、临时转录原位更新并自动滚动；兼容 `transcript/transcription/transcript_update/partial_transcript/final_transcript` 及 `text/content/transcript/delta/answer` 字段；Host 将连接和录音状态拆开，用户停止说话时只发送 `end_of_speech`，等待服务端 `type=done` 后再断开；停止会议只关闭转写并保留 session，会中和会后都可继续提问，新会议开始前才结束旧 session；摘要按真实字段生成可读文本，分析未完成时只显示处理状态和转录条数，不再显示原始 JSON。
- **服务器契约验证**：OpenAPI 已确认 session、status、transcript、understanding 和 end HTTP 接口；临时空 session 的 Host WSS 已确认连接成功，`end_of_speech` 后返回 `type=done`；调用 `/end` 后 Host WSS 返回连接失败，验证了会后问答必须保留活动 session。所有探测 session 均已结束，未输出 session ID 或会议内容。
- **构建验证**：ESP-IDF v5.5 标准构建完整通过，生成 `03_Clara_C6/build/Clara_C6.bin`（约 1.67 MiB）；应用分区剩余 72%，内部 D/IRAM 剩余约 128 KiB。该结果证明编译、链接和内存布局通过，尚未替代刷写后的实时转录、触摸和问答实测。
- **待处理**：当前 LVGL 配置未启用内置 `Source Han Sans SC CJK` 字体，中文转录可能显示为缺字方块。启用 CJK 字体需要修改本机 `sdkconfig.local` 并重新构建，属于独立配置边界，不能仅凭英文/ASCII 页面构建通过宣称中文显示已完成。服务器 `answer_audio` 事件目前也只有网络层解码，尚未接入扬声器播放；本轮只保证文字问答，不宣称语音回答已闭环。

## 2026-08-26：CJK 字体启用、按钮布局和实机烧录验证

- **现象**：虽然 `sdkconfig.local` 已加入 `CONFIG_LV_FONT_SOURCE_HAN_SANS_SC_16_CJK=y`，旧的 `build/sdkconfig.private` 仍记录了该选项为未设置，导致生成头文件和固件实际上没有包含 CJK 字体。按钮标签使用默认字体且没有明确的双轴布局，文字偏小并可能不在按钮正中。
- **根因**：ESP-IDF 的已有 sdkconfig 会保留显式值；重新运行 CMake 不会自动用更高优先级的 defaults overlay 覆盖旧的 `# CONFIG_... is not set`。因此私密 overlay 修改后必须移除/重建生成的 `build/sdkconfig.private`，再执行 `reconfigure`，才能让 Kconfig 重新合并。
- **修复**：保留凭据只在本机 `03_Clara_C6/sdkconfig.local`，删除旧生成配置后重新生成 `build/sdkconfig.private`；确认 `build/config/sdkconfig.h` 定义 `CONFIG_LV_FONT_SOURCE_HAN_SANS_SC_16_CJK`。转录和回答标签使用 `lv_font_source_han_sans_sc_16_cjk`，按钮标签明确使用 `lv_font_montserrat_16`；按钮采用 LVGL flex 双轴居中，固定为 Start/Stop `86x56`、问答 `136x56`、刷新 `56x56`，控制区高度 `64`，避免文字或状态变化引起布局漂移。
- **构建结果**：ESP-IDF 5.5 构建成功，`03_Clara_C6/build/Clara_C6.bin` 约 1.91 MiB；ELF 符号包含 `lv_font_source_han_sans_sc_16_cjk`，说明字体资源已链接进固件。`git diff --check` 无空白错误。
- **烧录与实机启动**：使用 `/dev/cu.usbmodem21201`、460800 波特率烧录成功。通过 USB-Serial/JTAG EN 复位后抓到完整启动日志：LCD SH8601、CST9217 触摸、ES8311/ES7210 编解码器和 LVGL 均初始化成功；Wi-Fi 成功关联之前的网络并获得 DHCP 地址；启动网络自测的 HTTP session 返回 200。原始本地日志保存为 `logs/cjk_flash_hardware_20260826.log`。
- **实机限制与后续**：本次串口证据未能替代人工目视确认 480x480 屏幕上的按钮像素位置，也未注入语音/触摸完成完整转写流程；应在板上点击 Clara、开始/停止会议并输入中文后确认字体和按钮视觉效果。启动自测中 WSS 在 `mbedtls_ssl_setup -0x7F00` 处失败，失败发生在 HTTP 200 之后且无崩溃/重启，属于运行期 SSL 建连资源不足，不能把它误判成 CJK 或 Wi-Fi 配置失败；后续应继续降低 WebSocket/TLS 峰值内存或延迟并发连接，并重新实测 WSS、实时转写和问答。

## 2026-08-26：点击 Start 提示 Transcription connection failed

- **现象与复现**：创建 meeting session 的 HTTP 请求返回 200，但紧接着转写 WebSocket 连接失败，UI 最终显示 `Transcription connection failed`。启动网络自测能稳定复现同一条 `clara_net_transcribe_connect()` 失败路径，排除了按钮事件、Wi-Fi 密码和 session 创建接口。
- **根因一（内部 RAM）**：实时转录 UI 同时常驻最终文本、临时文本和两份拼接后的 display 数组，共消耗约 14 KiB 内部 RAM。实机日志显示改动前 TLS 建连前最大连续块由曾经成功时的约 30 KiB 降到约 15 KiB，mTLS 最初在 `mbedtls_ssl_setup -0x7F00` 失败。WebSocket 客户端还会为 4096 字节应用缓冲预分配收发两块内存，进一步增加握手峰值。
- **根因二（PEM 参数语义）**：ESP WebSocket Client 的 `cert_len` 与 HTTP Client 语义不同：WebSocket 中非零 `cert_len` 会调用 DER 证书接口，而 Clara 提供的是 NUL 结尾 PEM 证书链。内存足以进入握手后，该错误表现为证书校验 `-0x2700`；HTTP 使用同一 PEM 可以返回 200，因此不能据此认定证书内容或服务器失效。
- **修复**：WebSocket 应用缓冲从 4096 降至 1024 字节（音频帧为 640 字节，文本帧已支持分片），私密 Kconfig 启用 `CONFIG_ESP_WS_CLIENT_ENABLE_DYNAMIC_BUFFER`；WSS 的 PEM 配置改为 `cert_len=0`。UI 删除两份 display 数组，最终转录/临时转录/回答缓存分别收敛为 1536/256/768/384 字节，通过 `lv_label_set_text_fmt()` 直接更新显示，从而保留 partial/final 行为并释放常驻内部 RAM。
- **实机回归**：ESP-IDF 5.5 构建及 `/dev/cu.usbmodem21201` 烧录成功。修复后 TLS 前可用内存约 46 KiB、最大连续块约 27 KiB；启动自测中转写 WebSocket `kind=0` 和 Host WebSocket `kind=1` 均连接成功，相关 HTTP 请求均返回 200，无 mbedTLS 错误、断言或重启。证据保存在本地 `logs/ws_ui_memory_fix_20260826.log`。该自测覆盖与 Start 相同的创建 session 和转写连接函数；屏幕点击后的持续音频上传和文字回显仍需用户实际说话确认。

## 2026-08-26：未操作却显示 Meeting link closed

- **现象**：设备启动后用户没有点击 Start，状态栏先后可能出现 `Listening`、`Ask Clara a question` 和 `Meeting link closed`，看起来像会议连接被异常关闭。
- **根因**：本机私密配置启用了 `CONFIG_CLARA_BOOT_SELF_TEST`。后台自测与正式会议复用同一套网络事件回调，会主动创建临时 session、连接转写/Host WebSocket，再主动断开和结束 session；而 UI 对所有 `TRANSCRIBE_DISCONNECTED` 事件都无条件显示 `Meeting link closed`，没有区分正式会议和诊断连接。这是后台诊断流程污染用户状态，不是触摸误报或服务端主动关闭用户会议。
- **修复**：验证网络链路完成后，在本机产品固件配置中关闭启动自测；同时为转写连接/断开 UI 事件增加 `s_meeting_active` 守卫，空闲状态不再响应后台或延迟到达的连接状态。正式 Start 成功后由会议流程明确设置 `Listening - live notes on`，避免事件先于会议状态置位造成遗漏。
- **实机验证**：ESP-IDF 5.5 构建和指定串口烧录成功。复位后静置 20 秒，LCD/LVGL、触摸、音频和 Wi-Fi 正常初始化；日志中没有启动自测、session/WebSocket、`Meeting link closed`、断言或重启。验证日志保存在本地 `logs/idle_state_fix_20260826.log`。

## 2026-08-26：Start 后约 7 秒自动显示 Meeting link closed

- **现象与实机证据**：点击 Start 后 session HTTP 返回 200，转写 WebSocket 成功连接，麦克风首帧也成功发送；持续上传约 7.3 秒后出现 `transport_poll_write(0)`，随后是 `close=0` / `errno=0` 的本地传输中止。证据保存在 `logs/meeting_link_closed_diagnostic_20260826.log`。
- **根因**：这次不是启动自测污染 UI，也不是服务端发来了 WebSocket Close frame。ESP WebSocket Client 的音频发送调用只给写操作 1000 ms；TCP/TLS socket 在这个窗口内不可写时，`poll_write` 返回 0，组件把单次写超时当成致命 transport error 并主动终止连接。UI 收到转写断开事件后才显示 `Meeting link closed`。动态 WebSocket 缓冲还会让 20 ms 音频帧每次发送都重新分配/释放 1024 字节 TX 缓冲，它是时序抖动的可疑加重因素，不写成已独立证实的根因。
- **修复**：本机私密配置关闭 `CONFIG_ESP_WS_CLIENT_ENABLE_DYNAMIC_BUFFER`，改用常驻 1024 字节收发缓冲，避免音频热路径上每帧 malloc/free；重建 `build/sdkconfig.private` 后确认生成配置为 `# CONFIG_ESP_WS_CLIENT_ENABLE_DYNAMIC_BUFFER is not set`。转写异常断开改由 action queue 串行处理：清理 WebSocket、停止会议采音、置 `s_meeting_active=false`、恢复 Start 按钮，并显示 `Connection lost • tap Start to retry`。用户主动 Stop 时先清除会议状态再断开，不再把正常关闭误判为异常掉线；会中 Host 正在录音时不强制停止共用音频任务。
- **验证边界**：清理临时诊断日志后，ESP-IDF 5.5 正式固件全量构建成功，已用 460800 波特率烧录到 `/dev/cu.usbmodem21201`。复位后显示、触摸、音频、LVGL 和 Wi-Fi 均正常初始化，连接 LDKJ 并获得 DHCP 地址，未出现崩溃、重启或空闲状态误报；日志保存在 `logs/meeting_link_recovery_final_20260826.log`。两次总计 165 秒的串口窗口均未收到 Start 交互，所以“持续音频超过 7.3 秒不再断线”和“实时文字回显”仍标记为待人工点击验证，不用构建/烧录成功代替功能结论。

## 2026-08-26：实时转写无文字并最终 Connection lost

- **现象**：Start 后 WebSocket 可以建立，麦克风也能持续读到 16 kHz、16-bit、mono PCM；但屏幕没有转写文字，运行一段时间后出现 `Connection lost • tap Start to retry`。串口根因表现为 `transport_poll_write(0)`、`close=0`、`errno=0`，不是服务端发送了明确的 WebSocket Close。
- **根因**：服务器正式客户端每次发送约 4096 个采样（约 256 ms）的文本帧：`{"type":"audio","data":"<Base64 PCM16>","speaker":"user"}`。Clara 原先每 20 ms 发送 640 字节原始 binary PCM，服务器虽然能解析并产生转录，但同一段固定语音的 A/B 结果是约 94 个 transcript 事件，而较大 JSON 帧约 8 个事件；大量小帧让服务端处理和 TLS 写队列长期积压，最终在 ESP WebSocket 单次写等待窗口内返回 0，组件将其作为致命 transport error 断开。也就是说，`transport_poll_write(0)` 是积压后的最终表现，1 秒/5 秒写超时不是完整根因；音频编码、Wi-Fi 凭据和 session 接口均已由 A/B 排除。
- **修复**：在 `clara_net` 内将 20 ms codec 帧聚合为 3200 字节（1600 个采样，约 100 ms），改用服务器正式 JSON/Base64 音频协议并固定 `speaker=user`；转写 `end`、Host `end_of_speech` 前先 flush 尾帧，连接建立和清理时清空聚合缓冲；转写与 Host 共用一个发送互斥锁，避免音频任务和结束动作并发修改缓冲。保持 16 kHz、16-bit、mono 采样契约，不改 I2S、DMA、任务优先级或栈。
- **软件验证**：用 macOS 合成的固定 16 kHz 语音做服务端 A/B：640-byte binary 与 3200-byte PCM 的 JSON/Base64 均建立 WSS、返回非空 transcript，且 `/transcript` 均落盘 2 条记录；JSON 版本消息数量显著降低。ESP-IDF 5.5 全量构建通过，产物 `03_Clara_C6/build/Clara_C6.bin`；新固件已烧录到 `/dev/cu.usbmodem21201`（460800）。
- **实机边界**：本轮烧录成功，但自动串口窗口未捕获到复位后的日志，尚未替代人工点击 Start、讲话并目视确认屏幕文字。因此“连接稳定超过原 7 秒”和“屏幕实时转写”在本次交付中标记为待人工实测；验证时应先打开串口，再点击 Start，讲话 5–10 秒，确认出现非空文字且状态不回落为 `Connection lost`。
- **发布前复核**：再次检查并修复了音频批次清理与 WSS 连接瞬断的竞态：清理/重连会先取得音频发送锁，Start 在等待 WSS 前置会议状态；ELF 已确认包含本轮新增协议和状态字符串。ESP-IDF 5.5 构建、烧录均成功，空闲实机 25 秒无启动自测、`Meeting link closed`、panic 或重启，LDKJ/DHCP 正常。可开始人工验证，但仍需实际讲话确认屏幕文字和长时稳定性。

## 2026-08-26：分片前仍然 Connection lost 的现场根因

- **现场证据**：最新实机日志显示 session HTTP 200、转写 WSS `kind=0` 连接成功，随后第一批音频发送立即返回 `err=257`（`ESP_ERR_NO_MEM`）。之后出现多次 `wifi:mem fail`，约 22 秒后 Wi-Fi `bcn_timeout`、断开 `reason=200`，WebSocket 被动断开，UI 显示 `Connection lost`。因此这次不是服务器拒绝音频，也不是分片协议本身的 Close。
- **根因**：上一版在 TLS 已占用内部堆后，第一次满 3200-byte PCM 批次需要申请约 4.3 KiB 连续 JSON/Base64 缓冲；虽然总剩余内存可能仍有，但最大连续块不足，申请失败。音频任务持续重试又叠加了 Wi-Fi 内存压力，最终触发热点 beacon 超时。
- **修复**：`ws_send_audio_json()` 改为固定小缓冲的 WebSocket 文本分片：发送 JSON 前缀、576-byte PCM（Base64 768 字节）分片、最后的 speaker 后缀和 FIN；整个音频消息仍是一个完整文本帧，服务端会在 FIN 后一次解析，但不再申请 4.3 KiB 连续内存。转写和 Host 共用的音频互斥锁继续保证分片序列不可交错。
- **验证边界**：分片版本已通过 ESP-IDF 5.5 构建并烧录，尚未完成该版本的人工讲话回归；下一次实测必须确认不再出现 `Audio stream send failed err=257`，并确认服务器 transcript 事件和屏幕文字均出现。

## 2026-08-26：分片版本实机测试仍被 Wi-Fi beacon 超时打断

- **本轮现场证据**：`logs/user_test_live_20260826.log` 显示第一次 Start 的 WSS 建连超时；第二次尝试成功出现 `WebSocket connected kind=0`、`Listening • live notes on` 和 `Audio stream started`。约 6.5 秒后先出现 `wifi:bcn_timeout`，随后 `reason=200` 断开，TLS 写入报 `ESP_ERR_MBEDTLS_SSL_WRITE_FAILED`，音频发送返回 `err=-1`，UI 最终显示 `Connection lost`。日志没有出现分片版首包 `err=257`，也没有收到 transcript。
- **根因归类**：本轮失败发生在音频流已经启动之后，直接触发点是 LDKJ 链路丢失（beacon timeout/disassoc），不是此前 4.3 KiB JSON 缓冲申请失败；TLS 写错误和 `Connection lost` 是 Wi-Fi 断开后的连带结果。设备随后重新关联 LDKJ 并重新获取 DHCP，说明凭据和 AP 可见性正常，但当前仍不能宣称实时转写链路已完成。
- **处理与边界**：已保留完整现场日志并停止串口监听以释放 `/dev/cu.usbmodem21201`；暂不在没有第二次人工讲话回归的情况下继续改动音频协议。下一次验证应在 Wi-Fi 重连并拿到 DHCP 后再点 Start，连续讲话 5–10 秒，同时确认不再出现 `bcn_timeout`、`Audio stream send failed`，并看到非空 transcript/UI 文本。

## 2026-08-27：Wi-Fi 断连真正根因是 SSID 弯引号

- **现象**：多轮烧录后板上始终 `reason=201 NO_AP_FOUND`，热点明明可见（扫描 RSSI -29）。
- **根因**：iPhone 热点名 `oxygen’s iPhone` 中是 Unicode 左弯引号 U+2018（3 字节，0xE28098），
  而配置里写的是 ASCII 直引号（1 字节）。固件逐字节匹配 SSID，永远不命中。
  扫描探针只打印数量/信道/RSSI 所以"能看见"，连接时才失败。修复为按扫描到的
  十六进制原样写入 `sdkconfig.local` 后立即连上并获得 IP（172.20.10.x 网段验证）。
- **教训**：iPhone 自动更正会把热点名里的 `'` 替换为 `’`；排查 SSID 匹配问题时必须比对十六进制。

## 2026-08-27：d472498 合入后的构建不可复现与静态内存重建

- **现象**：`git pull` 拉到 8-26 晚的固件大改提交（CA pin、音频全栈、boot self-test）后，
  从干净工作区重建失败：CJK 字体未声明；重建的 Wi-Fi 配置无法分配全部静态 RX 缓冲
  （`malloc buffer fail`，7~8/10），历史日志里同位置有 `wifi:mem fail`。
- **原因**：(1) 内存调优参数只存在于被 .gitignore 忽略的树内 `sdkconfig`；
  (2) CMake 的 SDKCONFIG_DEFAULTS 对已存在生成文件是"首写优先"，必须删除
  `build/sdkconfig.private` 再 reconfigure 才能让新增 defaults 生效；
  (3) LVGL 内置内存池以静态 BSS 存在，64 KiB 直接挤占 Wi-Fi 启动预算。
- **修复**（均已进入受版本控制的 `sdkconfig.defaults`）：从成功日志反推补上
  `CONFIG_LWIP_TCP_MSS=1240`、`CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=16`、
  `CONFIG_LWIP_IPV6=n`；并把 `CONFIG_LV_MEM_SIZE_KILOBYTES` 64→48。
  验证：boot 后 RX 10/10 分配成功、无 malloc buffer fail、UI ready。
- **附带发现**：8-26 成功会话所用 IDF 的 WiFi blob 为 b9f67df / PHY Jul-2026，
  与当前 v5.5.3 tag 的 4df78f2 / PHY Nov-2025 不同；当时应使用了更新的 IDF 分支且未保留。

## 2026-08-27：后端服务状态与遗留问题

- `clare.vinex.top`（Cloudflare 边缘正常、TLS 握手成功）源站读超时——**宿主机同样超时**，
  属服务器侧故障，与板端无关。板端 Start 会议在服务不可用时的表现：
  旧固件因堆耗尽瞬时返回（无网络尝试）；需在后端恢复后复测完整会议链路。
- 历史日志已确认的其他遗留问题（转自 intake 文档）：understanding 响应 255B 即触发
  `truncated=1`（16KB 上限内仍截断说明上游就截断了）；reason=200 beacon timeout 掉线
  由 PMF/省电引发（新固件已设 ps type:0 并限 11b/g/n）；`Error create websocket task`
  再次印证内存水位决定 WSS 可用性。

## 2026-08-27：进度收尾与当前边界

- 板端：新固件（d472498 + 内存修复配置）烧录验证，启动干净——Wi-Fi 静态 RX 缓冲 10/10、
  无 `malloc buffer fail`、UI ready、clara_audio ALC ready。弯引号 SSID 已按扫描十六进制
  原样写入本地私密配置并实测连通（iPhone 热点需停留在个人热点设置页保持广播）。
- 后端：`clare.vinex.top` Cloudflare 边缘可达、TLS 握手成功，但源站持续读超时
  （宿主机 curl 与板端一致），属服务器侧故障。后端恢复前，Start 会议链路无法端到端复测。
- 待办：(1) 后端恢复后点 Start 做完整会议链路验证；(2) 确认 understanding 响应上游截断
  （历史日志 truncated=1 是服务端已截断）；(3) 找回或对齐 8-26 成功会话所用 IDF 分支
  （WiFi blob b9f67df / PHY Jul-2026）。

## 2026-08-27（晚）：公司网络实测——板端会议链路已打通至服务端边界

- 配置改为 LDKJ（192.168.31.x，与 8-26 成功环境一致）后重烧。Wi-Fi 即连，
  boot self-test 自动执行。发现应用曾出现"加载后无任何串口输出"的冷挂死，
  `idf.py erase-flash` + 重烧后恢复且可复现正常启动；残留状态嫌疑指向 NVS，
  未进一步定位。
- 内存链路修复得到端到端验证：free8≈44KB（历史成功线 46KB），HTTP 会话创建
  反复返回 `status=200 bytes=82`、`session err=0`——此前板上从未达到该节点。
  关键新增调优：`CONFIG_MBEDTLS_SSL_IN_CONTENT_LEN=8192`（16KB 握手缓冲在该
  碎片化水位下装不下最大连续块，导致 `MBEDTLS_ERR_SSL_ALLOC_FAILED` 秒败）、
  `CONFIG_CLARA_BOOT_SELF_TEST=y`、LVGL 池最终 32KB。
- 剩余失败点收敛为：两条 WSS 通道握手稳定报 `-0x2880`
  （peer 证书签名类错误），而同一 host 的 HTTPS 同时段可用；
  Mac 侧同源全部超时；早前还观测到 TCP 连接耗时 0.0002s 的异常。
  综合判断为网络路径透明代理/SSL 干扰或后端边缘行为异常，属服务端/
  环境问题，不再是固件问题。处置建议：(1) 在干净网络复测同二进制；
  (2) 后端侧检查 Cloudflare WS 升级与回源；(3) 如需板上强验证可在
  ws config 打印对端证书指纹比对。

## 2026-08-27：本机 WSS 模拟器裁定——固件无罪，阻塞在后端回源

- 新增 `scripts/clara_wss_sim.py`：在本机复刻板端完整网络序列
  （提取 clara_ca_chain.h 固定链 -> POST /api/session -> 双通道 WSS，
  每条通道跑 pinned/system/none 三种信任对照），并打印服务器实发证书。
- 实测证据：(1) 无中间人——服务器下发真 LE 叶证书，指纹与固定链一致；
  (2) 板端同款 pinned 信任从本机成功完成 host 通道 WSS 握手并收到
  服务端 {"type":"ping"}；(3) 三次 POST 分别得 522 / 超时 / 200，
  后端处于 Cloudflare 回源超时（522）的间歇可通状态；
  (4) 板子与模拟器在同窗口内的成败模式一致（session 偶通、transcribe
  多为握手后无响应超时、WSS 时成时败）。
- 裁定：板端固件（含 TLS 缓冲调优与固定 CA）功能正确；剩余所有失败
  归因于 clare.vinex.top 源站不稳定。待服务端修复回源后在任意网络重测
  即可走完整会议链路。
- 附注：核对新代码确认 WSS 与 HTTP 都已改用 cert_pem=kClaraCaChainPem
  （bundle 不再参与），之前的 bundle 拒签问题在 d472498 已正确修复。

## 2026-08-27：当前进度总览（截至本条）

**已完成并验证：**
- 板端固件（d472498 + 内存调优）功能完整可用：启动干净（Wi-Fi 静态 RX 10/10、
  free8≈44KB 接近历史最佳水位）、LDKJ 网络即连、boot self-test 常开自动验证。
- TLS/WSS 信任链路端到端确认正确：session 曾在板上创建成功（200/err=0），
  本机同配置模拟器 host 通道握手成功并收到 {"type":"ping"}。
- 本机诊断工具链沉淀：backend_api_probe.py（协议探针）、clara_wss_sim.py
  （板端配置复刻 + 三信任模式对照 + 实发证书指纹检查）。

**当前唯一阻塞：**
- clare.vinex.top 源站间歇性不可达（Cloudflare 回源 522 / 悬挂超时）。
  板子与本机模拟器行为完全一致，证明与固件和网络环境无关。

**服务端恢复后的验收路径：**
1. 板子上电自动 boot self-test，串口看到 `self-test complete` 即四步全通。
2. 触摸屏 Start 会议 → 转 write 区出现 live notes → 点 Ask Clara 提问并应答。
3. 如 transcribe 通道仍单边异常而 HTTP 正常，优先查该路由的回源/长连接配置。

**遗留待办：**
- understanding 响应在服务端即被截断（truncated=1），需后端修。
- HOST_ANSWER_AUDIO 播放闭环待真实音频流验证。
- 8-26 成功会话所用更新版 IDF 分支未归档；如后续需要建议升级到统一版本。

## 2026-08-27：对照 confer-sum 两仓库后的结论

- 已阅读 clare-voice-api（后端）与 vocat/products/ws_meeting_demo（S3 已部署版），
  对照分析见 docs/confer-sum-reference-analysis.md。
- 服务端间歇不可达的最终判定：/health 间歇挂起 + 代码无同步阻塞 => 主机级
  资源问题或单 worker 事件循环被占满，需要服务器侧查看（容器限额/CPU/内存/同机负载）。
- C6 端协议实现与 API_DOC v2.1 一致；后续优先借鉴 S3 版的 MP3 按句播放、
  断线重连与 4009 冲突处理。

## 2026-08-27：S3 参考实现三项移植完成

1. **MP3 按句播放闭环（核心）**：曾尝试 vendor minimp3 流式解码，但 mp3dec_t
   等静态结构占 ~19KB，free8 掉到 16KB 直接饿死 TLS 握手，已回退。最终采用
   d472498 预留的 esp_audio_codec 路径：manifest 加 `esp_audio_codec ^2.4.0`
   （注意：registry 已无 1.x），clara_net 对 host 通道 answer_audio 做
   流式 base64 解码（4 字符跨片 carry + 768B 块），经 HOST_TTS_START/
   HOST_ANSWER_AUDIO/HOST_TTS_END 事件驱动 clara_audio_mp3_start/write/end。
   无 PSRAM 下不缓冲整句，解码器按需分配、播完释放，free8 仅降 ~1KB。
2. **403 会话重建**：WS ERROR 中 `esp_ws_handshake_status_code==403` 触发
   HOST_SESSION_REJECTED → main 新增 HandleHostRejected：结束旧 session、
   重建 session、重连 host、恢复录音。
3. **断线看门狗**：audio_task 连续发送失败 ~6s 触发 HandleMeetingDisconnect
   （等价于 S3 的 transcribe supervisor），消除"假 listening"状态。
   Finish/Stop 路径增加 mp3_end() 排空解码流。

验证：编译 0 错误；上板 RX 10/10、free8=43236、TTS 解码组件在位；
self-test 4 次尝试均失败于 err=28674——同时刻 Mac 访问 /health 也超时，
归因仍是后端源站间歇故障。真实 MP3 音频播放需等后端稳定后验证。

## 2026-08-27（深夜）：ZKY-GYLWLAP 网络"Meeting service unavailable"根因定位

- 用户在公司另一 AP（ZKY-GYLWLAP，BSSID 02:55:b1:c6:fa:2e 本地管理地址）下测试，
  session 全部失败 `err=28674`。三层探针（DNS/socket/connect + esp-tls）定位：
  DNS ✓、socket ✓、原始 TCP connect 首次 ✓ —— 但连接后 10~20s 出现
  `wifi:bcn_timeout → ap_probe_send → disassoc`，随后所有请求 `errno=113
  EHOSTUNREACH`。28674(0x7002)=ESP_ERR_ESP_TLS_CANNOT_CREATE_SOCKET 是链路
  断开后 socket/connect 失败的表象，不是根因。
- 对照：同时刻同 SSID 下 Mac（223.5.5.5 同 DNS）health 200 + session 创建成功。
  结论：AP 与板子之间的 beacon 保持异常（信号弱/干扰/AP 省电策略），非固件问题。
- 固件已加诊断：boot self-test 前置三层探针（保留，每次启动输出一行链路健康）。
- 待办：换位置/靠近 AP 复测；若仍 bc n_timeout，考虑 pmf required=true 启用
  protected keep-alive（该 AP 报 BSS max idle 291TU、protected keep alive:FALSE）。

## 2026-08-29：S3（1.75C）移植验收三绿 + 三连坑全记录

**背景**：Clare 从 C6（03_Clare_C6）移植到 Waveshare ESP32-S3-Touch-AMOLED-1.75C
（新工程 04_Clare_S3）。移植评估与 C6/S3 差异根因分析见
docs/why-c6-harder-than-s3.md 与 docs/clare-on-s3-1.75c-assessment.md。

**最终结果（已验收）**：LDKJ 网络下 boot self-test 三绿——session err=0（4.7s）、
transcribe err=0、host err=0（日志 logs/clare_s3_ldkj_boot_20260829.log）。
全程完整证书验证（固定链 cert_pem），未走 INSECURE 妥协。WSS 握手前堆水位
free8=6740027 / largest8=6553600（对比 C6 的 39KB/14.5KB，差 170 倍）。

### 坑 1：LVGL 断言重启环（`lvgl_bsp.cpp:74 disp != NULL`）

- **现象**：烧录后板子每 ~2 秒重启一次，串口报
  `esp_lvgl:disp: alloc primary buffer 46600 bytes failed` 后 assert。
- **根因**：S3 内部 RAM 主区域仅 95KB，CONFIG_LV_MEM_SIZE_KILOBYTES=64 的 LVGL
  内存池落在内部 RAM 后，466×50×2=46.6KB 的 DMA 帧缓冲分不到连续块。
- **走过的弯路**：把帧缓冲改成 WITH_PSRAM 宏（放 PSRAM）。PSRAM 缓冲不能被
  SPI 外设的 DMA 直读，spi_master 退化为在内部 RAM 申请 "priv TX buffer"
  中转，同样失败 → 每次 flush 报 `Draw bitmap failed: ESP_ERR_NO_MEM`，
  屏幕只剩顶部一条，其余全白（白屏的直接原因）。
- **正解（照抄 vocat 喵伴 S3 生产配置）**：内部 RAM 预算三件套——
  `CONFIG_LV_USE_CLIB_MALLOC=y`（LVGL 分配走 clib malloc，大块自动进 PSRAM）、
  `CONFIG_ESP_WIFI_IRAM_OPT=n`、`CONFIG_ESP_WIFI_RX_IRAM_OPT=n`
  （Wi-Fi 代码搬出 IRAM）。内部 RAM 主区域从 95KB 涨到 178KB，帧缓冲保持
  WITHOUT_PSRAM（内部 DMA RAM，QSPI 零拷贝直发，根本不需要中转）。
- **验证**：Wi-Fi 静态 RX 16/16（此前 12/16 报 malloc fail）、全日志零 E 级错误。

### 坑 2：Wi-Fi `reason=201 NO_AP_FOUND` 循环 —— 弯引号坑复发

- **现象**：Wi-Fi 驱动初始化正常，但扫描连接一直 reason=201。
- **根因**：这就是 8-26 记录过的 iPhone 热点弯引号坑（U+2018 = e2 80 98）
  复发——sdkconfig.local 里的 SSID 被写成了 ASCII 0x27。另一个意外发现：
  C6 工程的 sdkconfig.local 里 SSID 不知何时被截断成了 `oxygen`。
- **排查手法（已固化进 agent.md）**：`grep CLARE_WIFI_SSID build/sdkconfig.private | hexdump -C`
  逐字节核对，撇号必须是 `e2 80 98` 三字节。
- **解决**：`printf 'CONFIG_CLARE_WIFI_SSID="oxygen\xe2\x80\x98s iPhone"'` 写入
  正确字节。后按用户要求改用 LDKJ 网络，一次连上。

### 坑 3：板子进入重启环后 esptool 无法烧录

- **现象**：`Failed to connect to ESP32-S3: No serial data received`，
  换低波特率、多次重试均无效。
- **根因**：assert 重启环让芯片每 ~2 秒复位，USB-Serial/JTAG 桥随芯片反复
  重新枚举，esptool 的自动复位同步窗口永远对不上。
- **解决**：手动 ROM 下载模式——拔 USB → 按住 BOOT → 插回 USB → 2 秒后松开
  （黑屏是正常现象，ROM 不驱动 AMOLED）。进一次下载模式烧入修复固件后，
  板子恢复稳定运行，之后烧录不再需要手动按键。
- **教训**：遇到连接失败先判断板子是否在重启环（抓 5 秒串口看有没有周期性
  复位日志），不要无脑重试 esptool。

### S3 移植配置备忘（04_Clare_S3）

- 引脚/面板/触摸全部以小智板级包为准（XiaoZhi main/boards/waveshare/
  esp32-s3-touch-amoled-1.75/config.h 的 1_75C 分支）：I2C 14/15、
  I2S MCLK16/BCLK9/WS45/DOUT8/DIN10、PA 46、QSPI LCD CS12/PCLK38/D0-3=4-7、
  LCD_RST 1、触摸 RST2/INT11。
- CO5300 初始化必须 `esp_lcd_panel_set_gap(panel, 0x06, 0)`（列窗口从 6 开始）。
- 麦克风必须显式 `axp2101.enableALDO1()`（设电压≠使能，小智板级包有同样操作）。
- `ESP_TLS_USE_GLOBAL_CA_STORE` 在 IDF 5.5.x 不存在，C6 配置里那行是死配置，勿抄。
- LVGL 9.5 无 `LV_COLOR_16_SWAP`；esp_lvgl_adapter 对 SPI/QSPI 面板固定不交换
  字节，颜色问题不要往字节交换方向查。
- esp_websocket_client 锁 1.5.0（vocat 生产验证版）。
- IDF 环境：机器上只有 v5.5.3 + py3.9 venv，需
  `export IDF_PYTHON_ENV_PATH=$HOME/.espressif/python_env/idf5.5_py3.9_env`
  再 source export.sh。

---

## 2026-08-29（下午）S3 联网功能修复系列（04_Clare_S3）

下午围绕"会议录音 + Ask 问答 + 语音播报 + 总结"做全链路联调，连续挖出 7 个坑，
全部修复并通过免手动端到端自测（logs/clare_s3_vol130_boot_20260829.log 全绿）。

### 坑 4：自定义 LVGL 中文字库必须配对 LV_USE_FONT_COMPRESSED

- **现象**：中文转写显示为"口"，且第一个转写字出现时 Core 1 LoadProhibited
  panic（EXCVADDR=0），板子重启（logs/clare_s3_crash_repro2_20260829.log）。
- **根因**：lv_font_conv 生成的自定义字库（lv_font_clare_cjk_16.c，7540 字
  GB2312，bpp4）默认是**压缩格式**，而 sdkconfig 没开
  `CONFIG_LV_USE_FONT_COMPRESSED` → glyph 查找失败 → 渲染时 NULL memcpy。
- **解决**：sdkconfig.defaults 加 `CONFIG_LV_USE_FONT_COMPRESSED=y`。
- **教训**：任何 lv_font_conv 自定义字库进工程，必须同步检查该开关；
  生成命令见本文件上午条目（GB2312 全字符集 + Arial Unicode 源）。

### 坑 5：第二段语音时重启（WS 销毁与音频发送竞争）

- **现象**：第一段转写正常，说第二段话时 Core 0 LoadProhibited
  （EXCVADDR=0，栈在 mbedtls_ssl_write → ssl_check_ctr_renegotiate，
  logs/clare_s3_reboot_check_20260829.log）。
- **根因（双层）**：
  1. 应用层：ws_cleanup 的 stop/destroy 不持音频发送锁，音频任务发送途中
     客户端被释放（use-after-free）；
  2. 配置层：`MBEDTLS_DYNAMIC_BUFFER=y` 时读侧每帧释放 SSL RX 缓冲
     （in_ctr 暂时为 NULL），写侧并发写恰好踩中。
- **解决**：
  1. ws_cleanup 全程持 s_audio_send_lock 再做 stop/destroy；
  2. `MBEDTLS_DYNAMIC_BUFFER=n`（静态 SSL 缓冲，~20KB/连接落 PSRAM）+
     `MBEDTLS_SSL_RENEGOTIATION=n`（现代 TLS 服务器不重协商，直接移除崩溃点）。
- **验证**：修复后同类场景只返回可处理错误码，不再 panic。

### 坑 6：会议中途 Connection Lost（服务器主动掐线）

- **现象**：会议开始 20s~343s 不等后 WS 断连（收到 TLS fatal alert，
  logs/clare_s3_ask_diag3_20260829.log）。
- **根因（三条叠加）**：
  1. 音频消息以"分片帧"发送（640B 消息拆 3 帧、buffer_size=1024 又强制
     客户端再分片）——后端不能稳定重组 continuation 帧；
  2. 音频 JSON 多了 `"speaker":"user"` 字段——后端 schema 严格，违规消息
     积累后掐线（提问环节满屏 Network error 也是它逐条报错）；
  3. `network_timeout_ms=60000` 让客户端读阻塞 60 秒，PING/关闭处理全卡死。
- **解决**（全部对齐 vocat 参考实现 transcribe_ws.c）：
  - 音频消息改为 3200B PCM（100ms）一条、整 JSON 单帧发送（buffer_size=16384）；
  - 载荷逐字节对齐参考：`{"type":"audio","data":"<b64>"}`，无多余字段；
  - `network_timeout_ms=5000`、音频发送超时 500ms；
  - 转写通道改为**纯发送**（参考实现从不读；实时转写显示一并下线，
    转写/总结走 HTTP understanding）。
- **验证**：113 秒会议 + 多段问答无任何断连。

### 坑 7：会议中点 Ask 被 403，之后永远 "Clare Q&A unavailable"

- **现象**：会议中途点 Ask → host WS 握手 403（logs/clare_s3_ask_diag4_20260829.log）；
  失败后任意次再点 Ask 都报 "Clare Q&A unavailable"。
- **根因（两条）**：
  1. 服务器不允许同一会话两路并发音频流（喵伴参考做法：进提问先
     `transcribe_ws_pause()`，问完恢复）；我们当时双通道同播一路音频；
  2. host 客户端错误断连后没人调 ws_cleanup，`ctx->client` 残留 →
     后续每次 ws_connect 都 INVALID_STATE。
- **解决**：
  1. 麦克风同一时刻只喂一个通道：`s_host_recording=true` 要在
     **host_connect 握手之前**置位（握手数秒期间音频任务仍在跑），失败回退；
  2. `CLARE_NET_EVENT_HOST_DISCONNECTED` 事件里排队 Action::CleanupHost
     清理残留句柄（清理必须在主任务做，不能在 WS 任务里自毁）。
- **验证**：Ask E2E 自测 host 连接一次成功（403 gate 通过）。

### 坑 8：Network error 刷屏 + 无文字答案（JSON 重组 memcpy 虫）

- **现象**：点发送后满屏 "Network error"，答案文字/语音全无，但 <27 字节的
  短消息（如 done）正常（logs/clare_s3_tts_check_20260829.log）。
- **根因**：TTS 前缀探针消耗掉帧头 `need` 字节后，重组 memcpy 仍从
  `data->data_ptr`（帧首）拷贝而不是推进后的 `bytes` 指针 → 消息头部重复
  拼错 → 每条 >27B 的 host JSON 都解析失败 → report_error 刷屏。
  （短消息整体被探针缓冲原样保留，所以幸存。）
- **解决**：`memcpy(ctx->rx_buf + offset, bytes, frame_len);`（一处）。
- **教训**：这类"指针推进后又用原指针"的虫，用"每条收到的消息打一条
  recv type 日志"（对齐参考实现）能立刻现形——这条诊断日志已常驻代码。

### 坑 9：语音播报永远无声（播放链路从未初始化）

- **现象**：服务器 TTS 语音消息正常到达（tts stream begin/end），板子就是不出声。
- **根因**：`clare_audio_init_from_codec()` **从未被调用**（C6 工程同样没有，
  属于从 C6 继承的原始缺口）→ `clare_audio_is_ready()` 恒 false →
  mp3_start/write 全部 INVALID_STATE 静默丢弃。
- **解决**：app_main 里 codec 创建后立即 `clare_audio_init_from_codec(s_codec)`
  （esp_codec_dev_open 幂等，与音频任务共享句柄安全）。
- **验证**：开机自检音（embedded TTS MP3）播放 err=0；E2E 三段 TTS 流完整解码。
- **附加**：音量从 70 提到 130（esp_codec_dev >100 会映射为 ES8311 额外 DAC 增益）。

### 坑 10：新增静态大缓冲吃掉内部 RAM → WS 任务创建失败

- **现象**：固件加功能后 boot 自测全部 `Error create websocket task`，
  连接全挂；`free_internal=23435 largest_internal=8704`。
- **根因**：`SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY=y` **不覆盖**函数级/命名空间
  static——新加的 16KB 静态 JSON 缓冲 + 4.4KB 音频 JSON 全落在内部 .bss
  （0x3fc… 地址段，map 文件可查），10KB 的 WS 任务栈无处可分。
- **解决**：
  - 常驻音频缓冲（s_audio_json / 两个 batch）加 `EXT_RAM_BSS_ATTR`；
  - 临时大缓冲（总结 JSON）改为用时 `heap_caps_malloc(4096, MALLOC_CAP_SPIRAM)`，
    用完即 free。
- **验证**：free_internal 回到 50123 / largest 31744，WS 任务创建恢复。
- **教训**：S3 上任何 >1KB 的新 static/全局数组都必须显式决定去处
  （EXT_RAM_BSS_ATTR 或 PSRAM 分配）；烧录后看一眼 boot 日志的
  free_internal（健康线 ≥ 50KB / largest ≥ 30KB）再交付。

### 免手动端到端自测方法（boot_ask_e2e_test）

- 开机自测后自动跑完整问答：建会话 → host 连接（403 门禁）→ 开录音 10 秒
  → end_of_speech → 等 20 秒收答案/TTS → 拉 understanding → 结束会话。
- 录音窗口内用 Mac 外放一段预生成的中文问题 MP3（assets/ask_question.mp3）
  给板子麦克风"听"，全程无需触屏。
- 验收清单：transcription 到达、answer_text 流、tts stream begin/end、
  done、understanding err=0、无 panic/断连。
- 平时不挂接（费真实会话且每次播报），需要回归时在 action 分发里手动加回
  `boot_ask_e2e_test();`。

### 坑 11：第二次 Ask 必现 Network error + Connection Lost（看门狗误杀）

- **现象**：第一次 Ask 正常，第二次 Ask 时报 Network error，随后会议通道
  Connection Lost - tap Start to Retry。
- **根因（连锁反应，logs/clare_s3_round2_regression_20260829.log）**：
  1. host WS 握手期间（数秒），音频任务仍每 20ms 读麦克风并尝试向
     **尚未连上**的 host 通道发送 → 每次都返回 INVALID_STATE；
  2. 失败计数看门狗（300 次 ≈ 6-12 秒）在握手未完成时就触发
     HandleMeetingDisconnect → **误杀健康的会议通道**（Connection Lost）；
  3. 同时空转抢占 CPU，拖慢 TLS 握手（ESP_TLS_CONNECTION_TIMEOUT）→
     该轮 Ask 失败（Network error）。
- **解决**：握手期间（s_host_recording=true 但 s_host_connected=false）
  音频任务直接休眠 50ms 丢帧，不发送、不计看门狗（对齐参考做法：
  transcribe_ws_pause 先停 feed 任务再握手）。
- **验证**：双轮 Ask E2E（logs/clare_s3_round2_silent_20260829.log）两轮
  握手均 ~2.4s 成功，零错误、零看门狗触发。

### 坑 12：TTS 播报断断续续 + 答案只显示几个字

- **现象 A**：答案语音断续不连贯。**现象 B**：界面答案每次只显示最后几个字。
- **根因 A（三层叠加）**：
  1. MP3 解码+codec 写在 WS 客户端任务里同步进行：codec 写阻塞 → WS 读
     循环停摆 → TCP 反压 → 数据突发到达 → I2S 欠载（参考实现用 64 深队列
     + 独立播放任务解耦，mp3_player.c）；
  2. 每条 answer_audio 消息都触发 TTS_START/END → 每条消息重建解码器与
     播放状态，段间必然有缝；
  3. mp3_start 每次先清空缓冲再判断 active → 未播完的数据被丢弃。
- **根因 B**：服务器 answer_text 是增量片段流，UI 却每片 strlcpy 覆盖
  → 只显示最后一片。参考语义（ws_session.c）：新片段以已有文本开头
  = 全量快照→替换，否则=增量→追加。
- **解决**：
  1. 新增 96KB PSRAM 抖动环形缓冲 + 独立 tts_play_task（8KB 预缓冲开播），
     WS 任务只入队不解码（clare_audio.cpp）；
  2. 流跨消息保持：HOST_TTS_END 不再关闭流，流只在 HOST_DONE/停止路径
     关闭；mp3_start 已 active 时保留缓冲并清除残留 eos；
  3. mp3_end 不再强关解码器（播放任务自己排空后关闭，防 use-after-free）；
  4. 答案文本按参考语义累积（main.cpp host_answer_feed），UI 容量提至 2048。
- **验证**：11 段 TTS 消息共用 1 个播放任务连续播完（用户实听确认"不错"）；
  答案全文上屏。

### 坑 13：说话中途 Connection Lost（服务器主动掐线，需自动重连监督器）

- **现象**：会议健康进行 144 秒（转写持续回流）后，服务器发送 TLS 致命告警
  （mbedtls read -0x7780）主动断线；本地写侧撞拆除报 -0x6C00。
  历史上掐线点无规律（20s/83s/144s/343s），是服务器/代理侧策略，
  客户端无法预防（logs/clare_s3_user_acceptance_20260829.log）。
- **根因**：喵伴参考实现"很稳"的最后一块拼图是 `ws_session` **监督器**——
  被掐后自动重连，用户无感。我们之前一掐就死（Connection Lost 等用户点）。
- **解决**（main.cpp）：
  - `handle_meeting_disconnect_impl` 改为监督器：清场 → 退避 1s/2s/3s 重连
    3 次 → 会话被拒则重建会话续会 → 全失败才提示用户；
  - 新增 `s_transcribe_connected` 标志：断线/重连期间音频任务休眠丢帧
    （50ms），不发送、不喂看门狗（与坑 11 的握手期休眠同一模式）；
  - 排队重复的断连事件由 `if (s_transcribe_connected) return;` 防重复拆连。
- **验证**：固件已烧录、开机自测三绿（logs/clare_s3_reconnect_boot_20260829.log）；
  长会议掐线自愈场景待用户实测（服务器掐线不可强制复现）。

### 坑 14：TTS 播报跳过整句（解码错误=流死=缓冲清空）

- **现象**：答案应播"根据会议纪要…重要事项。另外，今晚别忘了…"，
  实际只播了首句"根据会议纪要"就直接跳到"另外…"，中间整句丢失。
- **根因**：服务器逐句生成语音（每句一条独立 MP3 消息）。某句 MP3 让
  解码器报错时，播放任务直接退出且 `mp3_active=false`；下一句的
  TTS_START 触发 mp3_start → 环形缓冲被整体清空 → 出错句连同缓冲残留
  全丢。另有次生问题：解码器"吃不下"的尾部字节（consumed==0）会被直接
  丢弃，可能破坏帧同步。
- **解决**（clare_audio.cpp tts_play_task）：
  1. 解码出错只**原地重置解码器**（close+open），流和缓冲保持，
     丢失的是毫秒级坏帧而非整句；连续 8 次无成功帧才放弃；
  2. 未消费尾部 memmove 续传到下次喂入（无损）；
  3. 整缓冲无进展时逐字节强制重同步，防死循环。
- **验证**：已编译通过；**截至提交时板子未插 USB，尚未烧录上板验证**，
  下次烧录后重点复测多句答案的播报完整性。
